spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springjpa
    username: postgres
    password: ashdhaker
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true